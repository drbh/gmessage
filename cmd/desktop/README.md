# gmessage desktop app

This app is a desktop and menubar app that makes to interact with the gmessage backend. By default it will be built and run, unless you specify otherwise.

```bash
make app
```